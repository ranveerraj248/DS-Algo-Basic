#include <bits/stdc++.h>
using namespace std;
void solve(){ 
    int n, a, b;
    cin >> n >> a >> b;
    string str;
    cin >> str;
    if(a < b){
        cout << (b + a) * n << endl;
    }else{
        vector<int> one;
        vector<int> zero;
        int no1, no0;
        

    }
}