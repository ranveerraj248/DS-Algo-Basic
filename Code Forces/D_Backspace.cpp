#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int i = 0;
    int j = 0;
    string str1, str2;
    cin >> str1 >> str2;
    size_t found = str1.find(str2[j]);
    if (found != string::npos)
    {
    }
    else
    {
        cout << "NO\n";
    }
}